# Cal follow-up — honest goals + drawOffice split

**Date:** 2026-09-06  
**Owners:** Dex (fix) · Cal (verify ask)  
**Parent:** `2026-09-06-cal-office-ui-verify.md`

## Fixes

1. **Goals no longer lie.** Removed Mark done. Progress is earned from
   real loft/desktop events (talk, drink, coffee, board, desk PC,
   Teams, Directory profile, agent replies). Auto-complete pays bucks
   when criteria are met. Goals app shows progress copy instead of a
   cheat button.

2. **`drawOffice.js` under Reed’s ~800 gate.** Split desk kit into
   `drawDeskKit.js` and loft props into `drawLoftProps.js`.
   `drawOffice.js` is orchestration + floor/world (~517 lines).

## Residual

Cal re-verify: earn a goal in-loft, confirm toast + bucks + shop buy
path; spot-check silhouettes still draw after the split.
